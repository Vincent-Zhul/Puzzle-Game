import ui.GameJFrame;
import ui.LoginJFrame;

public class App {
    public static void main(String[] args) {
        //The main method is the entry point of the program (程序的主入口)

        //If we want to use the class, we need to create an object of that class
        //创建一个对象
        new LoginJFrame();
    }
}
